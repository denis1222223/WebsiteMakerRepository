@model CourseProject.Models.UserSitesViewModel
@using CourseProject.Environment
@foreach (var site in Model.Sites)
{
    <tr>
        <td>
            <a href=@('/' + Model.UserName + "/" + site.Url + "/details")> @site.Name </a>
        </td>
        <td>
            @Html.DisplayFor(modelItem => Model.Rating)
        </td>
        <td>
            @Html.DisplayFor(modelItem => site.Url)
        </td>
    </tr>
}
</table>

